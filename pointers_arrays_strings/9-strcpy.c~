#include "main.h"
#include <stdio.h>
#include <string.h>

/**
* *_strcpy - print a function 
* 
* Result: string
*/

char *_strcpy(char *dest, char *src);
{
*ptr = dest;
 while ( *src != '0')
   {
     *dest = *src;
     dest++;
     src++;
   }
 *dest = '\0';
 return ptr;
}
